import { Injectable } from '@angular/core';
// import { Socket } from 'ngx-socket-io';

@Injectable({
  providedIn: 'root'
})
export class SocketService {
  // constructor(private socket: Socket) {}

  // // Send screen image to the backend
  // startScreenShare(imageData: string) {
  //   this.socket.emit('screen-share', { image: imageData });
  // }

  // // Receive screen images on the admin side
  // receiveScreenShare() {
  //   return this.socket.fromEvent<any>('screen-share-update');
  // }
}
